// import jwt from 'jsonwebtoken';

// const generateAuthToken = (id, firstName, is_admin) => {
//   const token = jwt.sign({ id, firstName, is_admin}, process.env.SECRET);
//   return token;
// };
// export default generateAuthToken;


